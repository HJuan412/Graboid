This directory contains training datasets.
Each dataset is stored in a directory named by the db_name argument in the database module
from DATA import DATA generates variables DATA.DATAPATH (path to DATA directory) and DATA.DBASES (list of existing database directories)